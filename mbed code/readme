Create new mbed project, simply replace main.cpp with this mbed code, update pin configuration according to your board. 

# Update
This is no more needed, program mbed and esp8266 separately. avoid AT commands, use JSON format for communication between ESP8266 and MBED.
